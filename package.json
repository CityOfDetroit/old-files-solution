{
  "name": "old-files-solution",
  "version": "2.0.0",
  "description": "Simple app to help us serve old files in drupal",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/CityOfDetroit/old-files-solution.git"
  },
  "scripts": {
    "start": "parcel index.html --https --no-cache --no-source-maps -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.js --no-cache --public-url '.' --no-source-maps -d build"
  },
  "author": "Edgar Montes",
  "license": "MIT",
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.14.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "cssnano": "^4.1.10",
    "sass": "^1.26.10"
  },
  "dependencies": {
    "isomorphic-fetch": "^3.0.0",
    "moment": "^2.29.2"
  }
}
